# Este archivo debe ejecutarse en el servidor remoto
# Comando: docker compose -f compose-database-server.yaml up -d

services:
  mysql:
    image: 'mysql:8.4'
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 'root_password_seguro'
      MYSQL_ROOT_HOST: '%'
      MYSQL_DATABASE: 'arena2'
      MYSQL_USER: 'arena2_user'
      MYSQL_PASSWORD: 'password_seguro'
      MYSQL_ALLOW_EMPTY_PASSWORD: 0
    volumes:
      - 'mysql_data:/var/lib/mysql'
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - mysqladmin
        - ping
        - '-ppassword_seguro'
      retries: 3
      timeout: 5s

  phpmyadmin:
    image: 'phpmyadmin:latest'
    ports:
      - '8080:80'
    environment:
      PMA_HOST: mysql
      PMA_USER: 'arena2_user'
      PMA_PASSWORD: 'password_seguro'
      UPLOAD_LIMIT: 300M
    restart: unless-stopped
    depends_on:
      - mysql

volumes:
  mysql_data:
    driver: local